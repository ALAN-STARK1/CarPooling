<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.example.carpooling.mapper.OrderMapper">

    <!-- order 是 MySQL 保留字，表名必须用反引号包起来：`order` -->
    <insert id="insert">
        INSERT INTO `order` (
            id,
            order_id,
            boardingPosition_id,
            dropOffPosition_id,
            passenger_id,
            driver_id,
            create_time,
            end_time,
            passenger_cost,
            driver_income
        ) VALUES (
            #{id},
            #{orderId},
            #{boardingPositionId},
            #{dropOffPositionId},
            #{passengerId},
            #{driverId},
            #{createTime},
            #{endTime},
            #{passengerCost},
            #{driverIncome}
        )
    </insert>
</mapper>
